# Core FastApi and Async utilities
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.7.0

# Testing & Dev
pytest==7.4.0

pytest-asyncio==0.23.5
httpx>=0.28.1
flake8>=7.0.0

# Google GenAI SDK (used by agent engine natively)
google-genai>=1.12.0
google-auth>=2.29.0
google-agents-cli>=0.1.0
google-adk[gcp]>=2.0.0
a2a-sdk[http-server]>=0.3.22

google-cloud-logging
python-dotenv

# Auth + per-user history (proxy)
firebase-admin>=6.5.0

# Multipart form parsing for the proxy's /upload endpoint
python-multipart>=0.0.9

# Causal reasoning engine (deterministic graph ops)
networkx>=3.3

# Statistical causal inference (deterministic identification + estimation).
# Runs in-process in the CausalEstimator node — it cannot go in Gemini's code
# sandbox or be an LLM tool (Vertex tool isolation). Pulls in pandas / scipy /
# statsmodels / scikit-learn / sympy. Only the agent image needs this; the
# proxy image does not ship src/.
# NB: require >=0.12 — 0.11.x calls networkx.d_separated, removed in networkx
# >=3.3 (this project pins networkx>=3.3), so 0.11.x fails identification.
dowhy>=0.12

# Causal discovery: data-driven correction of the LLM-asserted variable DAG
# (constraint-based PC + DirectLiNGAM) in src/causal/discovery.py, run inside
# the CausalEstimator when a dataset is available. Agent image only; pulls
# numpy/scipy/scikit-learn/pandas — mostly already present via dowhy. Imported
# lazily and degrades to a no-op when absent, so it is a soft requirement.
causal-learn>=0.1.3
